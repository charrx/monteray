import Dock from "../Dock/Dock";
import MenuBar from "../menubar/MenuBar";

const Desktop = () => {
  return (
    <div>
      <MenuBar></MenuBar>
      <Dock></Dock>
    </div>
  );
};

export default Desktop;
